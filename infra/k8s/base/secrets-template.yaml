# Secrets TEMPLATE — do not commit real values.
# In deployed environments this Secret is materialized from Vault via the
# External Secrets Operator (or `vault kv get` + kubectl apply in a pinch).
# All values below are placeholders; overlays never patch this file.
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: policy-twin
  labels:
    app.kubernetes.io/part-of: policy-twin
type: Opaque
stringData:
  DATABASE_URL: "mysql://CHANGEME:CHANGEME@mysql.data.svc.cluster.local:3306/policytwin"
  NEO4J_USER: "neo4j"
  NEO4J_PASSWORD: "CHANGEME"
  POSTGIS_URL: "postgres://CHANGEME:CHANGEME@postgis.geo.svc.cluster.local:5432/geo"
  S3_ACCESS_KEY: "CHANGEME"
  S3_SECRET_KEY: "CHANGEME"
  KEYCLOAK_CLIENT_SECRET: "CHANGEME"
  # Internal token used between the Node API and the Python services.
  SERVICE_MESH_TOKEN: "CHANGEME"
