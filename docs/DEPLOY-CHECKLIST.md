# Deploy Checklist — must-replace values (audit gap #40)

Every placeholder, default secret, OIDC issuer, TLS host, and must-replace
value in the repo, with file:line references. **Do not apply to staging/prod
until every box is checked.** Line numbers as of branch `feat-w1c-infra`.

## 1. `example.gov.ng` host placeholders (TLS hosts, OIDC URLs, email)

- [ ] `infra/k8s/base/ingress.yaml:24` — host `policy-twin.example.gov.ng`
- [ ] `infra/k8s/base/ingress.yaml:36` — TLS hosts entry (`secretName: policy-twin-tls`, line 37)
- [ ] `infra/k8s/base/ingress.yaml:18–19` — commented oauth2-proxy annotations `https://auth.example.gov.ng/…` (enable + substitute or delete)
- [ ] `infra/k8s/base/configmap.yaml:22` — `KEYCLOAK_URL: "https://idp.example.gov.ng"` (OIDC issuer base for the cluster)
- [ ] `infra/k8s/base/cert-manager.yaml:16` — ACME email `platform-ops@example.gov.ng`
- [ ] `infra/k8s/overlays/staging/kustomization.yaml:34,37` — `policy-twin.stg.example.gov.ng` (host + KEYCLOAK/issuer patches)
- [ ] `infra/k8s/overlays/staging/canary-ingress.yaml:39,51` — canary host + TLS hosts `policy-twin.stg.example.gov.ng`
- [ ] `infra/k8s/overlays/dev/kustomization.yaml:37,40` — `policy-twin.dev.example.gov.ng`
- [ ] `infra/k8s/overlays/prod/kustomization.yaml:62,65` — `policy-twin.example.gov.ng`

## 2. Kubernetes secrets template (never real values in git — wire Vault/ESO)

All `CHANGEME` in `infra/k8s/base/secrets-template.yaml` (consumed via
`infra/k8s/base/external-secrets.yaml` → Vault at
`https://vault.security.svc.cluster.local:8200`, line 16 — verify that
endpoint per cluster):

- [ ] `secrets-template.yaml:14` — `DATABASE_URL` user/password
- [ ] `secrets-template.yaml:16` — `NEO4J_PASSWORD`
- [ ] `secrets-template.yaml:17` — `POSTGIS_URL` user/password
- [ ] `secrets-template.yaml:18–19` — `S3_ACCESS_KEY` / `S3_SECRET_KEY`
- [ ] `secrets-template.yaml:20` — `KEYCLOAK_CLIENT_SECRET`
- [ ] `secrets-template.yaml:22` — `SERVICE_MESH_TOKEN`

## 3. OIDC issuer / client

- [ ] `api/utils/oidc.ts:46` — `OIDC_ISSUER` env must point at the real Keycloak realm (`https://<idp-host>/realms/policy-twin`); unset ⇒ OIDC disabled (Kimi OAuth fallback — see COMPLIANCE SEC-1)
- [ ] `infra/k8s/base/configmap.yaml:22` — `KEYCLOAK_URL` (see §1)
- [ ] `infra/k8s/base/secrets-template.yaml:20` — `KEYCLOAK_CLIENT_SECRET` (see §2)
- [ ] Confirm `KEYCLOAK_REALM` / `KEYCLOAK_CLIENT_ID` (`policy-twin` / `policy-twin-app` defaults in compose and overlays) match the realm import

## 4. Compose dev defaults — replace for any shared/staging compose use

`infra/docker/.env.example` (copy to gitignored `.env`):

- [ ] line 10–11 — `MYSQL_PASSWORD=policytwin`, `MYSQL_ROOT_PASSWORD=rootpassword`
- [ ] line 24–25 — `MINIO_ROOT_USER/PASSWORD=minioadmin`
- [ ] line 31 — `NEO4J_PASSWORD=policytwin`
- [ ] line 41 — `POSTGIS_PASSWORD=policytwin`
- [ ] line 45–46 — `KEYCLOAK_ADMIN=admin` / `KEYCLOAK_ADMIN_PASSWORD=admin`
- [ ] line 57–58 — `GRAFANA_ADMIN_USER/PASSWORD=admin`
- [ ] `infra/docker/docker-compose.yml:83` (+529, 600) — `LOADER_API_KEY` default `policytwin-loader-dev` (set env; loader rejects the default outside dev — see `docs/LOADER.md`)

## 5. Images & release pinning

- [ ] `infra/k8s/base/app.yaml:26`, `simulation.yaml:26`, `ai.yaml:26` — `ghcr.io/munisp/politicalintelligence/*:latest` → pin an immutable tag/digest per release (overlays patch tags; verify the registry/org is correct for your fork)
- [ ] `infra/k8s/overlays/staging/canary.yaml:35` — `:canary` tag updated by CI per release-candidate

## 6. TLS / cert-manager (SEC-5 — status 🟡 until staging apply)

- [ ] `infra/k8s/base/cert-manager.yaml` — ClusterIssuer ACME server/email (line 16) substituted; issuer applied to the cluster before ingress
- [ ] `infra/k8s/base/ingress.yaml:34–37` — TLS stanza host substituted; `policy-twin-tls` secret is cert-manager-issued, not hand-created
- [ ] Staging canary TLS: `infra/k8s/overlays/staging/canary-ingress.yaml:49–51`

## 7. Analytics / optional services

- [ ] Trino is profile-gated (`--profile lakehouse`); smoke: `TRINO_URL=http://localhost:8080 node scripts/trino-smoke.mjs`
- [ ] Dagster is profile-gated (`--profile orchestration`)
- [ ] OTel collector is default-on in compose but SDK export is env-gated (`OTEL_SDK_ENABLED`) — set per environment

_Generated by grepping for `example.gov.ng`, `CHANGEME`, default credentials,
and OIDC/TLS references. Re-run `grep -rn "example.gov.ng\|CHANGEME" infra/`
before each environment promotion._
